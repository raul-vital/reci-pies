<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/x-icon" href="/assets/favicon.ico" alt="pie icon">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Editing <%= recipeItem.title %></title>
</head>
<body>
     <%- include('./partials/navbar.ejs') %>
     <h1>Editing <%=recipeItem.title %></h1>
     <form action="/users/<%= user._id %>/recipes/<%= recipeItem._id %>?_method=PUT" method="POST">

        <label for="title">Title:</label>
        <input type="text" name="title" id="title"  value="<%= recipeItem.title %>" required >

       <label for="ingredients">Ingredients:</label>
        <input type="text" name="ingredients" id="ingredients" value="<%= recipeItem.ingredients %>" required>

       <label for="protein">Protein:</label>
        <input type="number" min="0" name="protein" id="protein" value="<%= recipeItem.protein %>" >

       <label for="carbs">Total Carbs:</label>
        <input type="number" min="0" name="carbs" id="carbs" value="<%= recipeItem.carbs %>">

       <label for="instructions">Instructions:</label>
       <input name="instructions" id="instructions" value="<%= recipeItem.instructions %>"required class="input-box" > 

       <button type="submit">Update Recipe</button>

     </form>
    
     <footer>Copyright &#169 2024 Reci-Pies</footer>
</body>
</html>